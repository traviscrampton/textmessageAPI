<h1>Messages</h1>

<h3>All the messages!</h3>

<ul>
  <% @messages.each do |message| %>
    <li><%= link_to message.id, message_path(message) %></li>
  <% end %>
</ul>

<%= link_to "New Message", new_message_path %>
