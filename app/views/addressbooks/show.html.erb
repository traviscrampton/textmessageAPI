<h1><%= @addressbook.owner %></h1>
  <ul>
    <% @addressbook.contacts.each do |contact| %>
      <li><%= link_to contact.name, contact_path(contact) %></li>
    <% end %>
  </ul>

<%= link_to "New Contact", new_addressbook_contact_path(@addressbook) %>
